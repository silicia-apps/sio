/*import {
  SioCoreAuthSessionModel,
  SioCoreAuthUserModel,
} from '../../auth';*/
//import { Observable } from 'rxjs';

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface SioCorePluginServiceModel {
  /*getUser(): Promise<SioCoreAuthUserModel | null>;
  getSession(): Promise<SioCoreAuthSessionModel | null>;
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  socket(session: string, user: string): Observable<any>;
  createSessionWithCredentials(
    username: string,
    password: string
  ): Promise<SioCoreAuthSessionModel | null>;
  createAnonymousSession(): Promise<SioCoreAuthSessionModel | null>;
  oAuth2Login(provider: string): void;
  logout(session: string): Promise<boolean>;*/
}
