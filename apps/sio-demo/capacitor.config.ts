import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'it.silicia.demo',
  appName: 'sio-demo',
  webDir: '../../dist/apps/sio-demo',
  bundledWebRuntime: false,
};

export default config;
